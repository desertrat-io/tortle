include "application.conf"
# everybody nice and tidy in here. just keep everyone together if testing is needed
play.evolutions.db {
  tortle_test {
    schema=tortle_test
  }
}
db {
  tortle_test {
    driver=com.mysql.cj.jdbc.Driver
    url="jdbc:mysql://localhost/tortle_test"
    username=${?DB_USERNAME}
    password=${?DB_PASSWORD}
  }
}

play.evolutions.db.tortle_test.autoApply=true

ebean.server.tortle_test.enhancement=["models.*"]